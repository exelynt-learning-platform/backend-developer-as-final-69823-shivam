# Copy to .env and adjust. All values have dev defaults in application.properties.
DB_URL=jdbc:postgresql://localhost:5434/bookingdb
DB_USER=booking
DB_PASS=booking
JWT_SECRET=replace-with-a-long-random-secret-at-least-32-bytes
JWT_EXPIRATION_MS=3600000
